# Environment Variables for Mahdra Backend

# ===========================================
# NEON CLOUD DATABASE (Profile: neon)
# ===========================================
# Utilisez ces variables avec le profil 'neon'
# Pour activer: spring.profiles.active=neon dans application.properties
NEON_DATABASE_URL=jdbc:postgresql://ep-calm-queen-a85ibf5e-pooler.eastus2.azure.neon.tech/neondb?sslmode=require&options=-c%20channel_binding=require
NEON_DATABASE_USERNAME=neondb_owner
NEON_DATABASE_PASSWORD=npg_rcqzwux0OJ8R

# ===========================================
# PRODUCTION DATABASE (Profile: prod)
# ===========================================
# Utilisez ces variables avec le profil 'prod'
# Pour activer: spring.profiles.active=prod dans application.properties
DATABASE_URL=jdbc:postgresql://your-prod-server:5432/mahdra
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-secure-password

# ===========================================
# NOTES IMPORTANTES
# ===========================================
# 1. Copiez ce fichier vers .env (ne committez JAMAIS .env dans git)
# 2. Remplacez les valeurs par vos vraies identifiants
# 3. Pour développement local, utilisez le profil 'dev' (aucune variable requise)
# 4. Pour Neon cloud, utilisez le profil 'neon'
# 5. Pour production, utilisez le profil 'prod'
