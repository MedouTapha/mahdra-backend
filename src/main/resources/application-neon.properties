# Neon Cloud Profile Configuration

# Neon PostgreSQL Database Configuration
spring.datasource.url=${NEON_DATABASE_URL:jdbc:postgresql://ep-calm-queen-a85ibf5e-pooler.eastus2.azure.neon.tech/neondb?sslmode=require&options=-c%20channel_binding=require}
spring.datasource.username=${NEON_DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${NEON_DATABASE_PASSWORD:npg_rcqzwux0OJ8R}
spring.datasource.driver-class-name=org.postgresql.Driver

# SSL Configuration for Neon
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000

# JPA/Hibernate Configuration
# IMPORTANT: Use 'validate' in production to prevent schema changes
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration
logging.level.com.mahdra.backend=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO

# Error handling
server.error.include-stacktrace=never
